# --- Core ML stack -----------------------------------------------------------
torch==2.2.0                 # PyTorch backend for training and inference
transformers==4.39.1         # HuggingFace Transformers library
tokenizers==0.15.2           # Matching tokenizers version for Transformers
datasets==2.19.0             # HuggingFace Datasets loader (20NG, RCV1, etc.)
scikit-learn==1.4.0          # Metrics, preprocessing, classic ML utilities
accelerate>=0.28.0           # Required by HuggingFace Trainer for device setup

# --- Scraping + Parsing ------------------------------------------------------
newspaper3k==0.2.8           # Article extraction (HTML → clean text)
beautifulsoup4==4.12.3       # HTML parsing and fallback scraping
lxml==5.2.2                  # Required XML/HTML parser backend
lxml_html_clean==0.2.0       # Provides lxml.html.clean module (fixes newspaper3k)
feedparser==6.0.10           # RSS feed ingestion

# --- DB + Config + Utilities -------------------------------------------------
pymongo==4.7.2               # MongoDB client for article storage
python-dotenv==1.0.1         # Loads environment variables from .env
PyYAML==6.0.2                # Reads config.yaml

# --- Optional text utilities -------------------------------------------------
nltk==3.9.2                  # Tokenization / stopwords (optional)
cssselect==1.3.0             # CSS selector support for HTML parsing
tldextract==5.3.0            # URL domain parsing (article metadata)
tinysegmenter==0.3           # Japanese tokenizer (required by newspaper3k)
jieba3k==0.35.1              # Chinese segmentation (required by newspaper3k)

# --- Web + Networking --------------------------------------------------------
requests==2.32.5             # HTTP requests for scraping and metadata fetch

# --- Pin supporting libraries (optional but recommended) ---------------------
tqdm==4.67.1                 # Progress bars for training and scripts
regex==2025.9.18             # High-performance regex library used by NLTK/Transformers
